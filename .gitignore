# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

*.db-journal
# Keep bundled demo DB so previews can open SQLite without provisioning.
# Local overrides still work; /tmp copy is used on read-only hosts.
/src/generated/prisma
/public/uploads/*
!/public/uploads/.gitkeep
